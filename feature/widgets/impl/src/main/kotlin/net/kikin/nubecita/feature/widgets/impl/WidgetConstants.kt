package net.kikin.nubecita.feature.widgets.impl

/**
 * Head depth the widgets render and the prefetcher decodes — a little beyond the
 * ~4–6 posts the largest responsive widget shows, so a resize finds thumbnails
 * ready. Bounds the per-feed decode count and the image cache. Shared so the
 * render count and the prefetch count never drift.
 */
internal const val MAX_WIDGET_POSTS = 8
