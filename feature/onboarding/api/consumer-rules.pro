# Consumer ProGuard rules contributed by :feature:onboarding:api. Intentionally empty —
# the api module only exposes a serializable NavKey, whose @Serializable plumbing is
# kept by kotlinx.serialization's own consumer rules.
